# GUI-specific dependencies for PDF Toolkit
# Install with: pip install -r requirements-gui.txt

# Core dependencies (from requirements.txt)
PyMuPDF>=1.23.0
pikepdf>=8.0.0
Pillow>=10.0.0
tqdm>=4.65.0

# GUI-specific dependencies
tkinterdnd2>=0.3.0      # Drag and drop support for tkinter
